apiVersion: v1
kind: Service
metadata:
  name: aws-dashboard-frontend-service
spec:
  type: NodePort

  selector:
    app: aws-dashboard-frontend

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
