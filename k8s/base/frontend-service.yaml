apiVersion: v1
kind: Service
metadata:
  name: aws-dashboard-frontend-service
  namespace: dashboard
spec:
  type: NodePort
  selector:
    app: aws-dashboard-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
